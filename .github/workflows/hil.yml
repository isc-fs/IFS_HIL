name: HIL Pipeline

on:
  push:
    branches: [main]
  pull_request:

jobs:
  hil:
    runs-on: [self-hosted, hil, raspberry-pi]
    steps:
      - uses: actions/checkout@v4
      - name: Run HIL job
        run: ./scripts/run_hil_job.sh
